/** Site-wide constants with no dependencies.
 *
 *  Its own module so client components can import the address without
 *  pulling db-portfolio — and the database driver behind it — into the
 *  browser bundle. */
export const SITE_EMAIL = "samantha.ahhee@gmail.com";
